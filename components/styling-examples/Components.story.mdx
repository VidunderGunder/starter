import { Meta } from "@storybook/addon-docs";
import Mantine from "./Mantine";
import Styled from "./Styled";
import CSSProp from "./CSSProp";
import { Group } from "@mantine/core";

<Meta />

# Components

Here's some example components using different styling paradigms:

<Group>
  <Mantine />
  <Styled />
  <CSSProp />
</Group>

They can also be mixed together, so have fun styling!
