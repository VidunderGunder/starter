/**
 * styled-components configuration
 *
 * Used with:
 * - Next.js (SWC): https://nextjs.org/docs/advanced-features/compiler#styled-components
 * - Storybook (Babel): https://styled-components.com/docs/tooling#babel-plugin
 */
const styledConfig = {
  cssProp: true,
  ssr: true,
  // meaninglessFileNames: ["index", "styles"],
};

module.exports = styledConfig;
